name: akshersetu_final
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  vibration: ^2.0.0
  camera: ^0.10.5+9
  image_picker: ^1.0.7
  crop_your_image: ^1.0.3
  flutter_tts: ^3.8.5
  http: ^1.2.0
  google_mlkit_text_recognition: ^0.13.0



  #  image_cropper: ^5.0.1

  #  google_mlkit_document_scanner: ^0.13.0  # check latest on pub.dev







  # Crop package (latest)
#  crop_your_image: ^2.0.0


  # File management
  path_provider: ^2.0.14

  # Animations
  lottie: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/background_image.png
